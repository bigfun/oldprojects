<?php

echo '<p>Logowanie</p>';

if (!empty($errors)) {
    echo '<ul>';
    foreach ($errors as $error) {
        echo '<li>'.$error.'</li>';
    }
    echo '</ul>';
}

echo form::open();

echo '<p>'.form::label('mail', 'Mail:').' '.form::input('mail').'<br / >';

echo form::label('pass', 'Hasło:').' '.form::password('pass').'</p>';

echo '<p>'.form::submit('submit', 'Zaloguj!').'</p>';

echo form::close();
